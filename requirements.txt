# Módulo [1] — reproducción del detector Extended Isolation Forest de ESA-ADB
# Python 3.9 (repo exige >=3.7,<3.10). numpy pinneado <2 (eif y pandas 1.5 lo requieren).
# eif compila con Cython<3 (su .pyx usa C++; Cython 3 lo rechaza).

# --- build (necesario ANTES de eif) ---
cython<3

# --- core ---
numpy==1.21.6
pandas==1.5.3
portion==2.4.1          # intervalos para la métrica ESA event-wise
scikit-learn==1.1.3     # dep transitiva del paquete de métricas ESA
tqdm                    # dep del paquete de métricas ESA

# --- detector ---
eif==2.0.2              # Extended Isolation Forest. Instalar con --no-build-isolation DESPUES de numpy

# --- interfaz / validación de contratos ---
pydantic>=2,<3         # DetectionResult (interfaz D7) valida en runtime, no solo documenta

# --- test ---
pytest
