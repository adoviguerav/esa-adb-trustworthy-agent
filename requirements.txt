# Módulo [1] — reproducción del detector Windowed iForest (subsequence_if) de ESA-ADB.
# subsequence_if = Isolation Forest ESTÁNDAR (Liu 2008, el que cita el paper), NO Extended IF.
# Implementado con pyod (sklearn por debajo). Corre en Mac CPU, sin GPU/Docker/conda.
# Python 3.9. Ver docs/ENV_DEVIATIONS.md para el detalle de versiones vs environment.yml de ESA.

# --- core ---
numpy==1.23.5           # pyod->numba exige >=1.22; RandomState estable => resultados idénticos
pandas==1.5.3
portion==2.4.1          # intervalos para la métrica ESA event-wise
scikit-learn            # dep de pyod y del paquete de métricas ESA
tqdm                    # dep del paquete de métricas ESA

# --- detector: subsequence_if ---
pyod==1.1.2             # Isolation Forest estándar sobre ventanas (subsequence_if/requirements.txt)

# --- métrica ESA (paquete timeeval vendorizado de esa-adb) ---
prts==1.0.0.3           # range-based P/R usado por timeeval other_metrics
statsmodels             # DatasetAnalyzer (solo metadata, no toca los datos)

# --- interfaz / validación de contratos ---
pydantic>=2,<3          # DetectionResult (interfaz D7) valida en runtime, no solo documenta

# --- test ---
pytest

# NOTA: `import timeeval` (para la métrica ESAScores) arrastra estas deps de su orquestación,
# aunque NO usemos Docker: docker (SDK), durations, dask[distributed]==2021.5.*, click==8.0.2,
# numpyencoder. Instaladas en el venv; ver docs/ENV_DEVIATIONS.md.
