"""Calibration: raw score distribution -> calibrated confidence + uncertainty band.

Skeleton only. Consume the full DetectionResult (needs score distribution + a
calibration split), produce per-point confidence + band, report a calibration metric.
"""
from __future__ import annotations

# TODO [2]: calibrate(result) -> DetectionResult with confidence + band (conformal/MAPIE).
