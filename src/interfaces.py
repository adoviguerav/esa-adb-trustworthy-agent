"""D7 contract — the fixed seam between the detector [1] and the trustworthy layer.

Skeleton only. Define the D7 types here (DetectionResult, Detection) so everything
downstream ([2] uncertainty, [3] attribution, [4] report) consumes ONLY this and the
detector stays interchangeable. Anchor to what ``repro/run_subsequence_if.py`` emits.
"""
from __future__ import annotations

# TODO: define DetectionResult (scores + ventana + datos) and Detection (per window).
