"""Generator: turn a single Detection into a grounded operator report.

Skeleton only. Consume ONLY window + responsible channels + uncertainty band (never
the raw full dataset); must refuse when unsure. Implement in [4].
"""
from __future__ import annotations

# TODO [4]: generate_report(detection) -> Report (grounded, refuse-when-unsure).
