"""Judge: audit a generated report against the detection evidence (D5 guardrail).

Skeleton only. Extract atomic claims, check each against window + channels + U,
return a verdict (approve / flag / block). Implement in [4].
"""
from __future__ import annotations

# TODO [4]: judge_report(report, detection) -> JudgeResult (approve / flag / block).
