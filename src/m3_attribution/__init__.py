"""[3] Attribution — which channels drove each detection.

Per-feature contribution over the flagged window (isolation-depth / perturbation based),
so the operator can orient their investigation without opening all channels. Output is
visual + tabular. Localises; it does NOT diagnose root cause (D3).
"""
