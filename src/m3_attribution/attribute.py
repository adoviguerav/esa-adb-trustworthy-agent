"""Per-channel attribution for a single flagged window.

Skeleton only. Consume one Detection (the window slice), return it with ``attribution``
filled (channel -> contribution). Fallback = raw magnitude ranking.
"""
from __future__ import annotations

# TODO [3]: attribute(detection) -> Detection with per-channel contribution.
