"""D7-conformant mock builders so [2]/[3]/[4] are testable before [1] is wired in.

Skeleton only. Add mock_detection_result() / mock_detection() here once interfaces.py
defines the D7 types. Structurally-real shapes, synthetic values, tests only.
"""
from __future__ import annotations

# TODO: mock_detection_result(...), mock_detection(...) once D7 types exist.
